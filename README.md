# ResuMint — Resume Builder

A locally-hosted single-page application for building tailored resumes. Built with Node.js, Express, SQLite, and vanilla JavaScript — run `npm install` then `npm start` and open `http://localhost:3000`.
